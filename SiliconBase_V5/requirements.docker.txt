# Docker 环境额外依赖
# 这些依赖在基础 requirements.txt 之外，专门为容器环境准备

# Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 缓存和存储
redis>=5.0.0

# 监控和健康检查
prometheus-client>=0.19.0
psutil>=5.9.0

# 安全和认证
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0

# 异步支持
httpx>=0.25.0
aiofiles>=23.0.0
