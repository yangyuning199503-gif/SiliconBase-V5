# SiliconBase V5 核心依赖
# 安装: pip install -r requirements-core.txt
# 
# 这是最小安装配置，只包含运行基础功能必需的依赖
# 如需完整功能，请安装 requirements-full.txt

# ========== 基础依赖 ==========
numpy>=1.24.0
pyyaml>=6.0
jsonschema>=4.0
requests>=2.28.0
psutil>=5.9.0

# ========== Token成本追踪 ==========
tiktoken>=0.5.0

# ========== Web框架 & API ==========
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0
websockets>=12.0
python-jose[cryptography]>=3.3.0  # JWT支持（安全启动必需）
passlib>=1.7.4                    # 密码哈希上下文
aiofiles>=23.0.0                  # 异步文件操作
bcrypt>=4.0.0                     # 密码哈希

# ========== 异步HTTP客户端（平台分发、数据采集） ==========
aiohttp>=3.9.0

# ========== 数据库 ==========
psycopg2-binary>=2.9.9
asyncpg>=0.29.0

# ========== 图像处理（轻量） ==========
pillow>=10.0.0
mss>=9.0.0

# ========== 缓存 & 存储 ==========
redis>=5.0.0

# ========== 定时任务 & 监控 ==========
apscheduler>=3.10.0
prometheus-client>=0.20.0

# ========== 文件监控 ==========
watchdog>=3.0.0

# ========== 平台适配器SDK ==========
python-telegram-bot>=20.0
discord.py>=2.3.0

# ========== 工具 & 实用 ==========
readerwriterlock>=1.0.0

# ========== Windows特定 ==========
pywin32>=305; platform_system=="Windows"
pyautogui>=0.9.54; platform_system=="Windows"

# ========== BTC表格输出 ==========
tabulate>=0.9.0
